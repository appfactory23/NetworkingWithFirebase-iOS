# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'NetworkingWithFirebase' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for NetworkingWithFirebase
  pod 'Firebase/Analytics'
  pod 'Firebase/Auth'

  target 'NetworkingWithFirebaseTests' do
    # Pods for testing
  end
end
